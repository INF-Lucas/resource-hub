# resource-hub

`resource-hub` is a categorized GitHub workspace for reusable resources.

Current structure:

- `skill/`: published skill packages

Current skills:

- `skill/epub-read`
- `skill/content-alchemy`
- `skill/pdf-read`

Notes:

- The original standalone repositories remain unchanged.
- The original Claude source skill for `pdf-read` also remains unchanged.
- Future non-skill content can be added as new top-level folders such as `docs/`, `templates/`, or other categories when needed.

Local workspace:

```text
/Users/lucas/GitHub
```
